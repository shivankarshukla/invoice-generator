import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.shivankarshukla.invoicegenerator',
  appName: 'Invoice Generator',
  webDir: 'dist',
};

export default config;
